import { AutoTabOnFullDirective } from './auto-tab-on-full.directive';

describe('AutoTabOnFullDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoTabOnFullDirective();
    expect(directive).toBeTruthy();
  });
});
